---
title: "Références"
description: ""
keywords: ""
changefreq: "monthly"
priority: "0.8"
---
h1 class="visuallyhidden" = "#{data.settings.site.title} - #{current_page.data.title}"
= partial 'views/global/header'
section class="content"

  h1 = data.content.references.title
  div class="wrapper grid panel--white big--padding"
    - data.content.references.projects.each_with_index do |project|
      article class="grid--two grid--margin panel--white"
        = link_to "projects/#{project.linkpath}" do
          picture class="reference--img"
            source srcset="#{project.imagebig}" media="(min-width: 1000px)"
            source srcset="#{project.imagemedium}" media="(min-width: 650px)"
            source srcset="#{project.imagesmall}" media="(min-width: 490px)"
            img srcset="#{project.imagexs}" alt="#{project.imagealt}"
          div class="reference--content"
              h1 class="subtitle" = project.clientname
              h2 class="h3" = project.title
    
= partial 'views/contactbox'
= partial 'views/footer'
